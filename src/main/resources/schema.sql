--drop table if exists member;
--create table member
--(
-- id bigint generated by default as identity,
-- email varchar(100),
-- member_real_id varchar(100),
-- password varchar(100),
-- nickname varchar(100),
-- role varchar(100),
-- primary key (id)
--);
--
--drop table if exists post;
--create table post
--(
-- id bigint generated by default as identity,
-- title varchar(100),
-- content varchar(255),
-- address varchar(255),
-- category varchar(255),
-- shop_name varchar(255),
-- member_id bigint,
-- board_id bigint,
-- primary key (id)
--);
--
--drop table if exists likes;
--create table likes
--(
-- id bigint generated by default as identity,
-- member_id bigint,
-- post_id bigint,
-- primary key (id)
--);
--
--drop table if exists reply;
--create table reply
--(
-- id bigint generated by default as identity,
-- member_id bigint,
-- post_id bigint,
-- primary key (id)
--);
--
--drop table if exists file;
--create table file
--(
-- id bigint generated by default as identity,
-- org_name varchar(255),
-- uuid_name varchar(255),
-- path varchar(255),
-- post_id bigint,
-- primary key (id)
--);
---- likes 테이블에 foreign key 제약 조건 추가
--alter table likes add constraint fk_likes_member foreign key (member_id) references member(id);
--alter table likes add constraint fk_likes_post foreign key (post_id) references post(id);
--
---- reply 테이블에 foreign key 제약 조건 추가
--alter table reply add constraint fk_reply_member foreign key (member_id) references member(id);
--alter table reply add constraint fk_reply_post foreign key (post_id) references post(id);
--
---- file 테이블에 foreign key 제약 조건 추가
--alter table file add constraint fk_file_post foreign key (post_id) references post(id);
--
---- post 테이블에 foreign key 제약 조건 추가
--alter table post add constraint fk_post_member foreign key (member_id) references member(id);
--
